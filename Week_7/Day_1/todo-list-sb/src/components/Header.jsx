import React from "react" // in newer versions of React, we don't need this

export default function Header() {
  return (
    <section>
      <h1>I am todo list title</h1>
    </section>
  )
}